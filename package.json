{
  "name": "collabryx",
  "version": "1.0.0",
  "description": "A real-time collaborative coding workspace built with React, Node.js, and Monaco Editor.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "dotenv": "^17.4.0",
    "mongoose": "^9.3.3"
  }
}
